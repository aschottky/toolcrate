{
  "name": "toolcrate",
  "private": true,
  "version": "1.0.0",
  "description": "Website Tear Down — $17 AI site audit landing page",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && touch dist/.nojekyll && gh-pages -d dist -t"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  }
}
